Install dependencies: type "npm install" without (",") on your terminal
Start the application: type "npm start" without (",") on your terminal


📂 Project Structure
main.js - Handles system events, window management, and IPC communication.

preload.js - Securely exposes protected backend APIs to the frontend.

renderer.js - Manages UI logic, state, and DOM updates.

store.js - Logic for reading and writing to the local JSON database.

styles.css - Custom styling for the gaming-inspired interface.

Note:
The app creates a user-preferences.json file in the user's AppData folder

📝 License
This project is MIT licensed.
